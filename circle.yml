machine:
  services:
    - rabbitmq-server

checkout:
  post:
    - git clone -b feature/encore-added-functionality git@github.com:stackstorm-exchange/ci.git ~/ci

dependencies:
  cache_directories:
    - ~/.cache/pip
    - ~/.apt-cache
  override:
    - ~/ci/.circle/dependencies

test:
  override:
    - ~/ci/.circle/test

deployment:
  index:
    branch: feature/encore-added-functionality
    owner: StackStorm-Exchange
    commands:
      - ~/ci/.circle/deployment
